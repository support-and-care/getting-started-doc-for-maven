{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Maven Documentation","text":"","path":["Maven Documentation"],"tags":[]},{"location":"authoring/","level":1,"title":"Plugins","text":"","path":["Plugins"],"tags":[]},{"location":"community/","level":1,"title":"Community","text":"","path":["Community"],"tags":[]},{"location":"get-started/","level":1,"title":"Getting Started","text":"","path":["Getting Started"],"tags":[]},{"location":"get-started/basic-project-sample/","level":1,"title":"Basic project example","text":"<p>A small step by step example project to cover the basics</p> <ul> <li>Structure of a Maven project</li> <li>executing Maven build</li> <li>Adding a dependency</li> <li>Adding a plugin</li> <li>Adding tests</li> </ul>","path":["Getting Started","Basic project example"],"tags":[]},{"location":"get-started/install-maven/","level":1,"title":"Download and Install Apache Maven","text":"<p>Maven is a command-line tool. Installing it means putting the <code>mvn</code> command on your <code>PATH</code> so you can run it from any terminal.</p> <p>This page walks you through:</p> <ol> <li>checking that you have a working Java installation,</li> <li>installing Maven (using a package manager or the official archive),</li> <li>verifying that everything works.</li> </ol> <p>If you are in a hurry and already know your way around your operating system, jump straight to the package-manager or manual install section.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#prerequisites-a-working-jdk","level":2,"title":"Prerequisites: a working JDK","text":"<p>Maven is itself a Java application, so you need a Java Development Kit (JDK) installed before you install Maven.</p> Maven version Required JDK to run Maven Maven 3.9.x (current stable) JDK 8 or later Maven 4.x (preview) JDK 17 or later <p>JDK vs. JRE</p> <p>You need a JDK, not just a JRE. The JDK includes the tools (such as <code>javac</code>) that Maven uses to compile your projects.</p> <p>You can use any modern JDK distribution, for example Eclipse Temurin, Amazon Corretto, Azul Zulu, Microsoft Build of OpenJDK, or the Oracle JDK.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#check-your-jdk","level":3,"title":"Check your JDK","text":"<p>Open a new terminal and run:</p> <pre><code>java -version\njavac -version\n</code></pre> <p>You should see something similar to:</p> <pre><code>openjdk version \"21.0.4\" 2024-07-16 LTS\njavac 21.0.4\n</code></pre> <p>If either command is not found, install a JDK first and then come back.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#set-java_home-recommended","level":3,"title":"Set <code>JAVA_HOME</code> (recommended)","text":"<p>Maven looks for Java by checking the <code>JAVA_HOME</code> environment variable first, then falling back to the <code>java</code> executable on your <code>PATH</code>. Setting <code>JAVA_HOME</code> explicitly makes your setup predictable and easier to debug.</p> macOS / Linux (bash, zsh)Windows (PowerShell) <p>Add the following to your shell profile (<code>~/.zshrc</code>, <code>~/.bashrc</code>, <code>~/.profile</code>, depending on your shell), replacing the path with your actual JDK location:</p> <pre><code>export JAVA_HOME=/path/to/your/jdk\nexport PATH=\"$JAVA_HOME/bin:$PATH\"\n</code></pre> <p>On macOS, you can let the system locate your JDK:</p> <pre><code>export JAVA_HOME=$(/usr/libexec/java_home)\n</code></pre> <p>Reload the shell (<code>source ~/.zshrc</code>) or open a new terminal.</p> <p>Set the variable for your user account:</p> <pre><code>[Environment]::SetEnvironmentVariable(\"JAVA_HOME\", \"C:\\Program Files\\Java\\jdk-21\", \"User\")\n</code></pre> <p>You can also set it through Settings → System → About → Advanced system settings → Environment Variables.</p> <p>Open a new PowerShell or Command Prompt window so the change takes effect.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#install-maven","level":2,"title":"Install Maven","text":"<p>There are two common ways to install Maven:</p> <ul> <li>With a package manager. This is the quickest path on most   systems and lets the package manager handle upgrades.</li> <li>From the official binary archive. This gives you full control   over the exact version and where Maven lives on disk.</li> </ul> <p>Pick whichever fits your environment. Both produce a working <code>mvn</code> command.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#install-with-a-package-manager","level":3,"title":"Install with a package manager","text":"macOSLinuxWindows <p>With Homebrew:</p> <pre><code>brew install maven\n</code></pre> <p>With SDKMAN! (handy if you also manage multiple JDKs):</p> <pre><code>sdk install maven\n</code></pre> <p>With MacPorts:</p> <pre><code>sudo port install maven3\n</code></pre> <p>The exact command depends on your distribution's package manager:</p> <pre><code># Debian / Ubuntu\nsudo apt update\nsudo apt install maven\n\n# Fedora / RHEL / CentOS Stream (dnf)\nsudo dnf install maven\n\n# Older RHEL / CentOS (yum)\nsudo yum install maven\n</code></pre> <p>Distro packages can lag behind</p> <p>Linux distributions sometimes ship an older Maven release. If you need a specific version, prefer the binary archive or SDKMAN!.</p> <p>With Chocolatey:</p> <pre><code>choco install maven\n</code></pre> <p>With Scoop:</p> <pre><code>scoop install main/maven\n</code></pre> <p>After the package manager finishes, skip ahead to Verify the installation.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#install-from-the-binary-archive","level":3,"title":"Install from the binary archive","text":"<p>This method works on any operating system and gives you the exact version published by the Apache Maven project.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#1-download-the-archive","level":4,"title":"1. Download the archive","text":"<p>Go to the Maven download page and download the latest binary distribution:</p> <ul> <li><code>apache-maven-&lt;version&gt;-bin.zip</code> — works on every platform, easiest   on Windows,</li> <li><code>apache-maven-&lt;version&gt;-bin.tar.gz</code> — convenient on macOS and Linux.</li> </ul> <p>Verify the download (optional but recommended)</p> <p>For production setups, verify the download against the published PGP signatures and checksums using the public KEYS file.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#2-extract-the-archive","level":4,"title":"2. Extract the archive","text":"<p>Extract it to a directory you control. A few common conventions:</p> <ul> <li>macOS / Linux: <code>/opt/apache-maven-&lt;version&gt;</code> or   <code>~/tools/apache-maven-&lt;version&gt;</code></li> <li>Windows: <code>C:\\Program Files\\Apache\\apache-maven-&lt;version&gt;</code> or   <code>C:\\Tools\\apache-maven-&lt;version&gt;</code></li> </ul> macOS / LinuxWindows (PowerShell) <pre><code>sudo tar -xzf apache-maven-3.9.16-bin.tar.gz -C /opt\n</code></pre> <p>This creates <code>/opt/apache-maven-3.9.16</code>.</p> <pre><code>Expand-Archive -Path apache-maven-3.9.16-bin.zip -DestinationPath C:\\Tools\n</code></pre> <p>This creates <code>C:\\Tools\\apache-maven-3.9.16</code>.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#3-add-maven-to-your-path","level":4,"title":"3. Add Maven to your <code>PATH</code>","text":"<p>Maven needs its <code>bin</code> directory on your <code>PATH</code> so the <code>mvn</code> command is available everywhere.</p> macOS / Linux (bash, zsh)Windows (PowerShell) <p>Add these lines to your shell profile (<code>~/.zshrc</code>, <code>~/.bashrc</code>, <code>~/.profile</code>):</p> <pre><code>export MAVEN_HOME=/opt/apache-maven-3.9.16\nexport PATH=\"$MAVEN_HOME/bin:$PATH\"\n</code></pre> <p>Reload your shell, for example:</p> <pre><code>source ~/.zshrc\n</code></pre> <p>Set the environment variables for your user. The snippet below is safe to re-run. It only appends to <code>Path</code> if Maven is not already there:</p> <pre><code>$mavenHome = \"C:\\Tools\\apache-maven-3.9.16\"\n[Environment]::SetEnvironmentVariable(\"MAVEN_HOME\", $mavenHome, \"User\")\n\n$userPath = [Environment]::GetEnvironmentVariable(\"Path\", \"User\")\nif ($userPath -notlike \"*$mavenHome\\bin*\") {\n  [Environment]::SetEnvironmentVariable(\"Path\", \"$userPath;$mavenHome\\bin\", \"User\")\n}\n</code></pre> <p>Or use the GUI: Settings → System → About → Advanced system settings → Environment Variables, then add <code>MAVEN_HOME</code> and append <code>%MAVEN_HOME%\\bin</code> to <code>Path</code>.</p> <p>Open a new terminal so the changes take effect.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#verify-the-installation","level":2,"title":"Verify the installation","text":"<p>Open a new terminal (so it picks up your new environment variables) and run:</p> <pre><code>mvn -v\n</code></pre> <p>You should see output similar to this:</p> <pre><code>Apache Maven 3.9.16 (2bdd9fddda4b155ebf8000e807eb73fd829a51d5)\nMaven home: /opt/apache-maven-3.9.16\nJava version: 21.0.4, vendor: Eclipse Adoptium, runtime: /opt/jdk-21\nDefault locale: en_US, platform encoding: UTF-8\nOS name: \"linux\", version: \"6.8.0\", arch: \"amd64\", family: \"unix\"\n</code></pre> <p>If you see this, Maven is installed and ready to use.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#troubleshooting","level":2,"title":"Troubleshooting","text":"<code>mvn: command not found</code> (or <code>'mvn' is not recognized...</code>) <p>The <code>mvn</code> command is not on your <code>PATH</code>. Confirm that:</p> <ul> <li>the <code>bin/</code> directory inside your Maven installation is on <code>PATH</code>,</li> <li>you opened a new terminal after editing your environment   variables.</li> </ul> <p>Run <code>echo $PATH</code> (macOS/Linux), <code>echo $env:PATH</code> (Windows PowerShell), or <code>echo %PATH%</code> (Windows Command Prompt) to inspect your current path.</p> <code>JAVA_HOME is not defined correctly</code> <p>Maven cannot find your JDK. Check that:</p> <ul> <li><code>JAVA_HOME</code> points to a JDK directory (not a JRE, and not the   <code>bin/</code> subdirectory),</li> <li>the directory exists and contains <code>bin/java</code> (or <code>bin\\java.exe</code>   on Windows).</li> </ul> <p>Run <code>echo $JAVA_HOME</code> (macOS/Linux), <code>echo $env:JAVA_HOME</code> (Windows PowerShell), or <code>echo %JAVA_HOME%</code> (Windows Command Prompt) to inspect the current value.</p> Wrong Java version is used <p><code>mvn -v</code> prints the JDK Maven is actually using. If it is not the one you expect, your <code>JAVA_HOME</code> or <code>PATH</code> is pointing at a different JDK. Update <code>JAVA_HOME</code> and open a new shell.</p> Behind a corporate proxy? <p>If Maven cannot download dependencies on first use, you may need to configure a proxy in <code>~/.m2/settings.xml</code>. See the Guide to Configuring Maven.</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/install-maven/#whats-next","level":2,"title":"What's next?","text":"<p>Maven is installed and verified. You are now ready to build your first project.</p> <p>Continue: Basic project sample →</p>","path":["Getting Started","Download and Install Apache Maven"],"tags":[]},{"location":"get-started/what-is-maven/","level":1,"title":"What is Maven (for)?","text":"","path":["Getting Started","What is Maven (for)?"],"tags":[]},{"location":"learning-paths/","level":1,"title":"Learning Paths","text":"","path":["Learning Paths"],"tags":[]},{"location":"usage/","level":1,"title":"User Guide","text":"","path":["User Guide"],"tags":[]}]}